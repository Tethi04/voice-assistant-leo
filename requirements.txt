# Core dependencies
SpeechRecognition==3.10.0
pyttsx3==2.90
python-dotenv==1.0.0
requests==2.31.0
beautifulsoup4==4.12.2

# Audio - platform specific (may need separate installation)
# pyaudio>=0.2.11  # Commented - install based on platform

# Optional for Streamlit web demo
streamlit>=1.28.0
